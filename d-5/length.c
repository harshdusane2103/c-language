#include<stdio.h>
#include<string.h>
main()
{
	char a[50];
	printf("Enter the name : ");
	scanf("%s",&a);
	int i,length=0;
	for(i=0;i<a[i];i++)
	{
	   length++;
	}
	printf("%d",length);
}
