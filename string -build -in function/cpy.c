#include<stdio.h>
#include<string.h>
main()
{
	char name[50];
	char name2[50];
    printf("Enter the name :");
    gets(name);
    name==name2;
      strcpy(name,name2);
    printf("%s",name2);
}
