#include<stdio.h>
main()
{
	int a,b;
	 
	 printf("Enter the value for a :");
	 scanf("%d",&a);
	 printf("Enter the value for b :");
	 scanf("%d",&b);
	  
	  if(a<b)
	  {
	  	printf("A is minimum !");
	  	
	  }
	  else
	  {
	  	printf("B is minimum !");
	  }
}
